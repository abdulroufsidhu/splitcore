# build outputs
splitcore/build/out/
*.so
*.dylib
*.a
*.h.gch
libsplitcore.h

# Dart/Flutter (any package in the monorepo, present or future)
.dart_tool/
.packages
build/
# ...but splitcore/build holds the native build *scripts*, not outputs.
# Only its out/ subtree is generated (excluded at the top of this file).
!splitcore/build/

# editor/OS
.idea/
.vscode/
*.swp
.DS_Store
pb_data/
server/server
